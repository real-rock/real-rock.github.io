# Design

DB 인터널 노트의 화면 규칙입니다. 값은 모두 `assets/css/extended/custom.css`에 있는 그대로입니다. 새 화면이나 요소를 만들 때 이 문서를 먼저 봅니다.

## 방향

- 큰 제목과 비대칭 배치(홈 7:5, 섹션 머리 3:9), 굵은 가로선 하나로 구역을 나눕니다.
- 기본 다크, 헤더 버튼으로 라이트 전환. 두 모드는 같은 위계를 가집니다.
- 강조색은 하나(오렌지)만 씁니다. 카드, 배지, 알약 모양 태그, 섹션 번호 라벨, 대문자 eyebrow는 쓰지 않습니다.
- 글 화면은 왼쪽 연재 목차 / 본문 / 오른쪽 이 글의 목차 3단입니다. 순서대로 읽는 연재라서 지금 위치가 늘 보여야 합니다.

## 색

| 토큰 | 다크 | 라이트 | 쓰임 |
|---|---|---|---|
| `--bg` | `#101113` | `#fbfbfa` | 페이지 바탕 |
| `--bg-2` | `#17181b` | `#f2f2f0` | 코드 블록, 인용, 접이식 목차 |
| `--tx` | `#ececee` | `#151516` | 제목, 본문 강조 |
| `--tx-2` | `#a3a4ab` | `#55565c` | 설명문, 보조 텍스트 |
| `--tx-3` | `#85868d` | `#6b6c72` | 날짜, 메타, 준비 중 항목 (4.5:1 이상) |
| `--line` | `#2a2b30` | `#e4e4e1` | 가는 구분선 |
| `--strong` | `#d8d8dc` | `#1c1c1e` | 구역을 나누는 굵은 가로선, 표 머리 |
| `--ac` | `#ff8a4c` | `#c2410c` | 강조색 하나: 연재 번호, 현재 위치, 링크 밑줄, 포커스 |

- 본문 텍스트는 `--tx`를 바탕과 12% 섞어 조금 눌러 씁니다.
- 코드 구문 색(`--k`, `--s`, `--m`, `--f`)은 코드 안에서만 씁니다.
- 순수한 검정(`#000`)과 흰색(`#fff`)은 쓰지 않습니다.

## 서체

- 본문과 제목은 Wanted Sans(한글 포함)이고, 코드는 JetBrains Mono입니다.
- 세리프는 쓰지 않습니다. 강조는 같은 서체의 굵기로만 합니다.

| 역할 | 크기 | 굵기 | 자간 |
|---|---|---|---|
| 홈 제목 | clamp 40-60px | 800 | -0.045em |
| 글·페이지 제목 | clamp 34-52px | 800 | -0.045em |
| 연재 이름 (홈) | 30px | 800 | -0.035em |
| 본문 h2 | 26px | 800 | -0.03em |
| 본문 h3 | 20.5px | 700 | -0.02em |
| 본문 | 17px / 1.85 | 400 | |
| 목록 제목 | 18px | 600 | |
| 메타 | 14.5px | 400 | |

## 배치

- 최대 폭은 1180px이고, 좌우 여백은 40px(모바일 20px)입니다. 본문 한 줄은 최대 700px입니다.
- 모서리는 8px 하나로 통일합니다. 인라인 코드와 복사 버튼만 4-6px입니다.
- 구역 사이는 `--strong` 1px 가로선 하나로 나눕니다. 목록 행 사이는 `--line`으로 위쪽 선만 긋습니다.
- 분기점은 두 개입니다.
  - 1100px 이하: 오른쪽 목차 대신 본문 위 접이식 목차
  - 760px 이하: 한 단, 연재 목차도 본문 위 접이식

## 요소

| 요소 | 파일 | 규칙 |
|---|---|---|
| 헤더 검색창 | `_partials/header.html`, `js/header-search.js`, `css/extended/header-search.css` | 메뉴 오른쪽 끝의 밑줄형 입력창(200px, 포커스 시 밑줄 강조색). 입력 중 결과 최대 5개, 고른 항목은 제목이 강조색, 일치 부분은 강조색 밑줄. `/` 키로 포커스, 검색 페이지에서는 숨김. 760px 이하에서는 메뉴 아래 한 줄 전체 |
| 연재 목차 블록 | `_partials/series_block.html` | 번호는 강조색, 준비 중은 `--tx-3`와 "준비 중" 글자. `limit`(홈 5편), `detail`(주제 표시), `hideTitle` |
| 글 목록 행 | `_partials/post_row.html` | 제목 + 오른쪽 날짜. 목록 페이지에서만 요약 표시 |
| 글 3단 배치 | `single.html` | 연재 글이면 `has-series`, 소제목이 있으면 `has-toc` |
| 왼쪽 연재 목차 | `single.html`, `_partials/extend_footer.html` | 접기 버튼(Phosphor `sidebar-simple`). 접힌 상태는 `localStorage.rail-series`에 저장하고 `:root[data-rail="off"]`로 적용 |
| 오른쪽 이 글의 목차 | `single.html` | Hugo `.TableOfContents`. 읽는 소제목은 강조색 |
| 이전·다음 글 | `_partials/post_nav_links.html` | 연재 글은 연재 순서로 연결. 아이콘 없이 "이전 글/다음 글" |
| 표 | `_markup/render-table.html` | 좁은 화면에서 표 안에서만 가로 스크롤 |
| 다이어그램 | `_partials/extend_head.html` | Mermaid `base` 테마를 다크/라이트 토큰에 맞춤 |

## 모션

- 모션은 연재 목차를 접고 펼 때 한 번만 씁니다. 320ms `cubic-bezier(0.22, 0.8, 0.24, 1)`이고, 사용자가 누를 때만 움직입니다. 페이지를 불러올 때는 움직이지 않습니다.
- 그 밖에는 링크와 제목 색이 150ms 동안 바뀌는 것뿐입니다.
- `prefers-reduced-motion`에서는 전환을 모두 끕니다.

## 하지 않는 것

- 카드 격자, 색 배지, 알약 모양 태그, 섹션 번호 라벨, 대문자 eyebrow
- em-dash(—)와 en-dash(–). 범위도 하이픈으로 씁니다.
- 한 줄에 가운데점(·) 두 개 이상. 메타 정보는 간격으로 구분합니다.
- 직접 그린 SVG 아이콘. 아이콘은 Phosphor만 씁니다.
- 강조색 두 개 이상, 그라데이션 글자, 빛나는 효과
