# Design

Jin's 블로그의 화면 규칙입니다. 값은 모두 `assets/css/extended/custom.css`(공통, 글 화면)와 `home.css`(홈, 카테고리 페이지)에 있는 그대로입니다. 새 화면이나 요소를 만들 때 이 문서를 먼저 봅니다.

## 방향

- 홈은 익숙한 블로그 모양입니다. 단색 커버 아래에 썸네일이 붙은 글 목록, 오른쪽에 프로필, 연재 진행, 카테고리 트리, 태그 사이드바를 둡니다.
- 글은 DB 카테고리(`categories`)와 하위 분류(`subcategory`)로 나누고, 순서대로 읽는 글은 연재(`series`)로 묶습니다.
- 기본 다크, 헤더 버튼으로 라이트 전환. 두 모드는 같은 위계를 가집니다.
- 강조색은 하나(오렌지)만 씁니다. DB 색은 썸네일 안에서만 씁니다.
- 글 화면은 왼쪽 연재 목차 / 본문 / 오른쪽 이 글의 목차 3단입니다. 순서대로 읽는 연재라서 지금 위치가 늘 보여야 합니다.

## 색

| 토큰 | 다크 | 라이트 | 쓰임 |
|---|---|---|---|
| `--bg` | `#101113` | `#fbfbfa` | 페이지 바탕 |
| `--bg-2` | `#17181b` | `#f2f2f0` | 코드 블록, 인용, 접이식 목차 |
| `--tx` | `#ececee` | `#151516` | 제목, 본문 강조 |
| `--tx-2` | `#a3a4ab` | `#55565c` | 설명문, 보조 텍스트 |
| `--tx-3` | `#85868d` | `#6b6c72` | 날짜, 메타, 준비 중 항목 (4.5:1 이상) |
| `--line` | `#2a2b30` | `#e4e4e1` | 가는 구분선 |
| `--strong` | `#d8d8dc` | `#1c1c1e` | 구역을 나누는 굵은 가로선, 표 머리 |
| `--ac` | `#ff8a4c` | `#c2410c` | 강조색 하나: 연재 번호, 현재 위치, 카테고리 줄, 진행 막대, 링크 밑줄, 포커스 |

- 본문 텍스트는 `--tx`를 바탕과 12% 섞어 조금 눌러 씁니다.
- 코드 구문 색(`--k`, `--s`, `--m`, `--f`)은 코드 안에서만 씁니다.
- 썸네일 색(`home.css`)은 카테고리 `_index.md`의 `color` 값으로 고릅니다. 썸네일 바탕은 이 색을 `--bg-2`와 22% 섞어 씁니다.

| 값 | 다크 | 라이트 | 카테고리 |
|---|---|---|---|
| `pg` | `#5b8fc7` | `#336791` | PostgreSQL |
| `mg` | `#5fb35a` | `#3f8f3a` | MongoDB |
| `my` | `#3aa6c2` | `#0f7490` | MySQL |
| `rd` | `#e5645a` | `#c8352b` | Redis |
| `cm` | `#9a8ce0` | `#6b5bb5` | DB 비교, 공통 개념 |
| `nt` | `#85868d` | `#6b6c72` | 공지 등 그 밖의 글 |
- 순수한 검정(`#000`)과 흰색(`#fff`)은 쓰지 않습니다.

## 서체

- 본문과 제목은 Wanted Sans(한글 포함)이고, 코드는 JetBrains Mono입니다.
- 세리프는 쓰지 않습니다. 강조는 같은 서체의 굵기로만 합니다.

| 역할 | 크기 | 굵기 | 자간 |
|---|---|---|---|
| 홈 커버 제목 | 38px (모바일 30px) | 800 | -0.045em |
| 글·페이지 제목 | clamp 34-52px | 800 | -0.045em |
| 연재 이름 (시리즈 페이지) | 30px | 800 | -0.035em |
| 본문 h2 | 26px | 800 | -0.03em |
| 본문 h3 | 20.5px | 700 | -0.02em |
| 본문 | 17px / 1.85 | 400 | |
| 홈 글 항목 제목 | 20px (모바일 17px) | 700 | -0.025em |
| 목록 제목 | 18px | 600 | |
| 썸네일 글자 | 19px (1100px 이하 16px, 모바일 14px) | 800 | -0.04em |
| 메타 | 14.5px | 400 | |

## 배치

- 최대 폭은 1180px이고, 좌우 여백은 40px(모바일 20px)입니다. 본문 한 줄은 최대 700px입니다.
- 모서리는 기본 8px입니다. 썸네일은 10px, 사이드바 상자는 12px, 인라인 코드와 복사 버튼은 4-6px입니다. 태그와 카테고리 칩은 알약 모양입니다.
- 홈과 카테고리 페이지는 본문과 288px 사이드바의 2단(간격 48px)입니다. 글 항목은 본문 + 오른쪽 208px 썸네일(16:10)입니다.
- 글 화면의 구역 사이는 `--strong` 1px 가로선 하나로 나눕니다. 글 목록 머리 아래는 `--tx` 2px 선, 항목 사이는 `--line` 선입니다.
- 사이드바 상자는 `--bg-2` 바탕이고 테두리와 그림자는 없습니다.
- 분기점은 두 개입니다.
  - 1100px 이하: 오른쪽 목차 대신 본문 위 접이식 목차. 홈 사이드바는 248px, 썸네일은 168px
  - 760px 이하: 한 단, 연재 목차도 본문 위 접이식. 홈은 사이드바가 목록 아래로 내려가고, 카테고리 트리 대신 목록 위 가로 칩, 썸네일은 104px 정사각형

## 요소

| 요소 | 파일 | 규칙 |
|---|---|---|
| 헤더 검색창 | `_partials/header.html`, `js/header-search.js`, `css/extended/header-search.css` | 메뉴 오른쪽 끝의 밑줄형 입력창(200px, 포커스 시 밑줄 강조색). 입력 중 결과 최대 5개, 고른 항목은 제목이 강조색, 일치 부분은 강조색 밑줄. `/` 키로 포커스, 검색 페이지에서는 숨김. 760px 이하에서는 메뉴 아래 한 줄 전체 |
| 홈 커버 | `home.html` | `--bg-2` 단색 띠(배경만 화면 끝까지). 왼쪽 블로그 이름과 소개, 오른쪽 글·연재·카테고리 수 |
| 글 항목 (썸네일) | `_partials/post_item.html` | 홈과 카테고리 페이지. 카테고리 줄(강조색), 연재 글이면 "연재 이름 n/전체" 표시, 제목, 요약 2줄, 날짜와 태그 3개(카테고리와 같은 태그 제외), 오른쪽 썸네일 |
| 썸네일 | `_partials/thumb.html` | 이미지 대신 카테고리 색 바탕에 글자로 그린 표지. 큰 글자는 front matter `thumb`, 없으면 짧은 제목. 작은 글자는 `subcategory`(연재 글이면 "인터널 01") |
| 사이드바 | `_partials/side.html` | 프로필(첫 글자 아바타, `params.profile`, 소개와 RSS 버튼), 연재 진행 막대, 카테고리 트리(글 수, 하위 분류), 태그 12개 |
| 카테고리 페이지 | `categories/term.html`, `categories/taxonomy.html` | 카테고리 글 전체. `?sub=운영`이면 하위 분류만 걸러 보여 주고 제목을 "PostgreSQL › 운영"으로 바꿈. 글이 없는 카테고리는 목록에서 뺌(`_partials/categories.html`) |
| 페이지 이동 | `_partials/pages.html` | 번호 버튼, 현재 페이지는 `--tx` 바탕 |
| 연재 목차 블록 | `_partials/series_block.html` | 시리즈 페이지. 번호는 강조색, 준비 중은 `--tx-3`와 "준비 중" 글자. `limit`, `detail`(주제 표시), `hideTitle` |
| 글 목록 행 | `_partials/post_row.html` | 글, 태그 목록 페이지. 제목 + 오른쪽 날짜, 요약 표시 |
| 글 3단 배치 | `single.html` | 연재 글이면 `has-series`, 소제목이 있으면 `has-toc` |
| 왼쪽 연재 목차 | `single.html`, `_partials/extend_footer.html` | 접기 버튼(Phosphor `sidebar-simple`). 접힌 상태는 `localStorage.rail-series`에 저장하고 `:root[data-rail="off"]`로 적용 |
| 오른쪽 이 글의 목차 | `single.html` | Hugo `.TableOfContents`. 읽는 소제목은 강조색 |
| 이전·다음 글 | `_partials/post_nav_links.html` | 연재 글은 연재 순서로 연결. 아이콘 없이 "이전 글/다음 글" |
| 표 | `_markup/render-table.html` | 좁은 화면에서 표 안에서만 가로 스크롤 |
| 파비콘 | `static/favicon.svg` 외, `hugo.toml` `params.assets`, `_partials/extend_head.html` | `--bg-2` 둥근 사각형(모서리 14/64)에 JetBrains Mono ExtraBold `J`(`--tx` 다크값)와 강조색 밑줄 커서. J는 글꼴에서 윤곽선으로 뽑은 path. 세로획과 커서는 16px에서 2px로 픽셀 격자에 맞춤. apple-touch-icon만 모서리 없이 꽉 채움 |
| 다이어그램 | `_partials/extend_head.html` | Mermaid `base` 테마를 다크/라이트 토큰에 맞춤 |

## 모션

- 모션은 연재 목차를 접고 펼 때 한 번만 씁니다. 320ms `cubic-bezier(0.22, 0.8, 0.24, 1)`이고, 사용자가 누를 때만 움직입니다. 페이지를 불러올 때는 움직이지 않습니다.
- 그 밖에는 링크와 제목 색이 150ms 동안 바뀌는 것과, 글 항목에 마우스를 올리면 썸네일이 200ms 동안 2px 떠오르는 것뿐입니다.
- `prefers-reduced-motion`에서는 전환을 모두 끕니다.

## 하지 않는 것

- 카드 격자, DB 색을 입힌 배지, 섹션 번호 라벨, 대문자 eyebrow
- 그라데이션 배경 커버, 그림자로 띄운 상자
- em-dash(—)와 en-dash(–). 범위도 하이픈으로 씁니다.
- 한 줄에 가운데점(·) 두 개 이상. 메타 정보는 간격으로 구분합니다.
- 직접 그린 SVG 아이콘. 아이콘은 Phosphor만 씁니다.
- 강조색 두 개 이상, 그라데이션 글자, 빛나는 효과
